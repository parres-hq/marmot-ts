import PlaceholderExample from "../../components/placeholder-example";

const TITLE = "Proposals";
const FEATURES = `
Create various proposal types (add, remove, update)
View pending proposals in group
Test proposal validation and processing
Simulate proposal lifecycle
`;

export default function Proposals() {
  return <PlaceholderExample title={TITLE} features={FEATURES} />;
}
