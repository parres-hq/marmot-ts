import PlaceholderExample from "../../components/placeholder-example";

const TITLE = "Commit Timeline";
const FEATURES = `
Visualize group state changes over time
Show commit history with timestamps
Display proposal-to-commit relationships
Track group epoch progression
`;

export default function CommitTimeline() {
  return <PlaceholderExample title={TITLE} features={FEATURES} />;
}