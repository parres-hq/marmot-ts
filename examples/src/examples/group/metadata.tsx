import PlaceholderExample from "../../components/placeholder-example";

const TITLE = "Marmot Group Metadata";
const FEATURES = `
- Encode MIP-01 group metadata to a hex string
- Decode MIP-01 group metadata from a hex string
`;

export default function GroupMetadata() {
  return <PlaceholderExample title={TITLE} features={FEATURES} />;
}
