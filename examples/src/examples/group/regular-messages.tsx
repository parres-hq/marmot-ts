import PlaceholderExample from "../../components/placeholder-example";

const TITLE = "Regular Group Messages";
const FEATURES = `
Send encrypted application messages
Receive and decrypt group messages
Display message history with sender identities
`;

export default function RegularGroupMessages() {
  return <PlaceholderExample title={TITLE} features={FEATURES} />;
}
