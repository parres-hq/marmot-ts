import PlaceholderExample from "../../components/placeholder-example";

const TITLE = "Key Package Relay List";
const FEATURES = `
- Add and remove relays from the key package relay list
- Show supported NIPs for relays (NIP-40, and NIP-70)
`;

export default function KeyPackageRelays() {
  return <PlaceholderExample title={TITLE} features={FEATURES} />;
}
