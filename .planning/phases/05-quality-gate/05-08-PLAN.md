---
phase: 05-quality-gate
plan: 08
type: execute
wave: 4
depends_on: [05-07]
files_modified: [.planning/phases/05-quality-gate/05-CI-EVIDENCE.json, scripts/validate-quality-ci.mjs, .planning/phases/05-quality-gate/05-QUALITY-GATE.md]
autonomous: true
requirements: [QA-01, QA-02]
must_haves:
  truths:
    - "Authenticated GitHub evidence identifies immutable run/job IDs for all six normal and extended rows at tested_source_sha."
    - "The final gate combines CI and dossier attestations for tested_source_sha while recording later attestation SHAs separately."
  artifacts: [".planning/phases/05-quality-gate/05-CI-EVIDENCE.json", "scripts/validate-quality-ci.mjs", ".planning/phases/05-quality-gate/05-QUALITY-GATE.md"]
  key_links: ["GitHub run headSha and every dossier tested_source_sha match; final evidence records its distinct attestation commit SHA without a self-SHA claim."]
---
<objective>Retrieve, validate, and consolidate authoritative hosted runtime evidence for tested_source_sha.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/05-quality-gate/05-07-SUMMARY.md
@.planning/phases/05-quality-gate/05-DOSSIER-SCHEMA.json
@scripts/validate-quality-dossiers.mjs</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Retrieve and machine-validate immutable CI evidence</name>
  <files>.planning/phases/05-quality-gate/05-CI-EVIDENCE.json, scripts/validate-quality-ci.mjs</files>
  <read_first>.github/workflows/tests.yml, .planning/phases/05-quality-gate/05-RUNTIME-EVIDENCE.md, .planning/phases/05-quality-gate/05-07-SUMMARY.md</read_first>
  <behavior>- Authenticated retrieval selects a completed successful Tests run whose headSha equals tested_source_sha, not current HEAD or attestation_sha.
- Six unique job rows resolve Node 20/22/24, Deno 2, Bun latest/1.1 with immutable run/job IDs and URLs.
- Downloaded logs prove pnpm 10, frozen install, normal suite, extended config, resolved versions, exits, and counts.
- Missing/duplicate rows, mutable branch-only URLs, wrong SHA, absent extended output, or failed jobs cause nonzero validation.</behavior>
  <action>Use authenticated `gh run list --workflow tests.yml --commit &lt;tested_source_sha&gt; --json databaseId,headSha,status,conclusion,url` to select the run, `gh api repos/{owner}/{repo}/actions/runs/{run_id}/jobs --paginate` for immutable job IDs/URLs, and `gh run view {run_id} --log` plus `gh run download {run_id}` when artifacts exist. Store parsed evidence, not credentials, in JSON. Validate run headSha equals tested_source_sha and logs contain explicit runtime plus pnpm version output for every job, frozen install, and both suites. Record the commit containing CI evidence later as `ci_attestation_sha` in 05-08-SUMMARY.md/final downstream tracking; do not put a self-referential SHA inside its own commit.</action>
  <verify><automated>node scripts/validate-quality-ci.mjs .planning/phases/05-quality-gate/05-CI-EVIDENCE.json</automated></verify>
  <done>Machine validation proves all six jobs ran both suites successfully at tested_source_sha using immutable run/job IDs.</done>
</task>
<task type="auto">
  <name>Task 2: Seal the final quality gate</name>
  <files>.planning/phases/05-quality-gate/05-QUALITY-GATE.md</files>
  <read_first>.planning/phases/05-quality-gate/05-CI-EVIDENCE.json, .planning/phases/05-quality-gate/05-PROOF-V2-DOSSIER.md, .planning/phases/05-quality-gate/05-KEY-PACKAGE-LIFETIME-DOSSIER.md, .planning/phases/05-quality-gate/05-TAG-CARDINALITY-DOSSIER.md, .planning/phases/05-quality-gate/05-SAFE-AAD-DOSSIER.md</read_first>
  <action>Write the final record from validated JSON/dossiers: tested_source_sha, spec/MDK gitlinks, dossier attestation_sha from 05-06-SUMMARY, six runtime rows with immutable IDs/URLs and both suite results, local smoke clearly secondary, four parity dossiers, failure/repair ledger, and QA mapping. State that the final record's containing commit is a later attestation and have 05-08-SUMMARY.md record that SHA after commit; never assert the document contains its own SHA. Mark complete only if both validators bind primary evidence to tested_source_sha.</action>
  <verify><automated>node scripts/validate-quality-dossiers.mjs &amp;&amp; node scripts/validate-quality-ci.mjs .planning/phases/05-quality-gate/05-CI-EVIDENCE.json</automated></verify>
  <done>The milestone gate is backed by complete hosted and cross-implementation primary evidence at one SHA.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| GitHub API/logs → release record | Branch aliases, wrong runs, or incomplete logs can overstate runtime coverage. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-09 | Spoofing | CI evidence | critical | mitigate | Authenticated API, exact head SHA, immutable run/job IDs, log validation. |
| T-05-10 | Repudiation | final gate | critical | mitigate | Persist parsed evidence and validate all source artifacts at one SHA. |
| T-05-SC | Tampering | package installs | high | mitigate | CI logs must prove pnpm 10 and frozen installation. |
</threat_model>
<verification>Both dossier and CI validators bind primary evidence to the same tested_source_sha.</verification>
<acceptance_criteria>QA-01 has authenticated six-runtime normal+extended evidence and QA-02 has four independently reproducible dossiers, all at one SHA.</acceptance_criteria>
<success_criteria>No release claim depends on local substitutes, mutable URLs, stale dossiers, or unvalidated prose.</success_criteria>
<output>Create `05-08-SUMMARY.md` when done.</output>
