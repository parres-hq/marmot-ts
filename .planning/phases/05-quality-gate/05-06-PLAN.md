---
phase: 05-quality-gate
plan: 06
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05]
files_modified:
  - scripts/validate-quality-dossiers.mjs
  - .planning/phases/05-quality-gate/05-DOSSIER-SCHEMA.json
  - .planning/phases/05-quality-gate/05-PROOF-V2-DOSSIER.md
  - .planning/phases/05-quality-gate/05-KEY-PACKAGE-LIFETIME-DOSSIER.md
  - .planning/phases/05-quality-gate/05-TAG-CARDINALITY-DOSSIER.md
  - .planning/phases/05-quality-gate/05-SAFE-AAD-DOSSIER.md
autonomous: true
requirements: [QA-02]
must_haves:
  truths:
    - "All four D-04 dossiers attest one immutable tested_source_sha plus its spec/MDK SHA tuple; later evidence commits are separately identified as attestation SHAs."
    - "A machine validator rejects missing commands, outputs/digests, negative controls, or independent-oracle provenance."
  artifacts: ["scripts/validate-quality-dossiers.mjs", ".planning/phases/05-quality-gate/05-DOSSIER-SCHEMA.json"]
  key_links: ["Validator parses all four dossiers, requires one tested_source_sha, verifies that commit's gitlink SHAs, and never equates an evidence commit with the tested source."]
---
<objective>Converge every parity dossier on one immutable tested source commit and enforce its evidence schema mechanically.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/05-quality-gate/05-PROOF-V2-DOSSIER.md
@.planning/phases/05-quality-gate/05-KEY-PACKAGE-LIFETIME-DOSSIER.md
@.planning/phases/05-quality-gate/05-TAG-CARDINALITY-DOSSIER.md
@.planning/phases/05-quality-gate/05-SAFE-AAD-DOSSIER.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Define and enforce the dossier evidence schema</name>
  <files>.planning/phases/05-quality-gate/05-DOSSIER-SCHEMA.json, scripts/validate-quality-dossiers.mjs</files>
  <read_first>.planning/phases/05-quality-gate/05-CONTEXT.md, .planning/phases/05-quality-gate/05-RESEARCH.md, .planning/phases/05-quality-gate/05-PROOF-V2-DOSSIER.md, .planning/phases/05-quality-gate/05-KEY-PACKAGE-LIFETIME-DOSSIER.md, .planning/phases/05-quality-gate/05-TAG-CARDINALITY-DOSSIER.md, .planning/phases/05-quality-gate/05-SAFE-AAD-DOSSIER.md</read_first>
  <behavior>- Validates exact tested_source_sha/spec/MDK SHAs and rejects mixed/stale tuples while permitting later attestation commits.
- Requires executable Rust extraction and TS verification commands, captured output or digest, negative-control command/result, and oracle-independence declaration in each dossier.
- Requires D-05's source classification and D-06/b4649c01 review where applicable.
- A deliberately removed required field causes nonzero exit.</behavior>
  <action>Create a versioned JSON schema and dependency-free Node validator for the four dossier evidence blocks. Require the same 40-hex `tested_source_sha`, `marmot_sha`, and `mdk_sha` in every dossier. Validate submodule provenance from the tested commit itself using `git ls-tree &lt;tested_source_sha&gt; refs/marmot refs/mdk`, not current working-tree HEAD. Require exact command strings executed from a detached worktree at tested_source_sha; nonempty captured output/digest; passing negative control; oracle generator language/path distinct from TS subject; tag oracle classification; and b4649c01 fields for lifetime/SafeAAD. Permit current HEAD to be a later evidence commit. Include a validator self-test using temporary copies.</action>
  <verify><automated>node scripts/validate-quality-dossiers.mjs --self-test</automated></verify>
  <done>The validator accepts complete evidence and demonstrably rejects every required-field class.</done>
</task>
<task type="auto">
  <name>Task 2: Attest all four dossiers against one pre-existing tested source commit</name>
  <files>.planning/phases/05-quality-gate/05-PROOF-V2-DOSSIER.md, .planning/phases/05-quality-gate/05-KEY-PACKAGE-LIFETIME-DOSSIER.md, .planning/phases/05-quality-gate/05-TAG-CARDINALITY-DOSSIER.md, .planning/phases/05-quality-gate/05-SAFE-AAD-DOSSIER.md</files>
  <read_first>scripts/validate-quality-dossiers.mjs, .planning/phases/05-quality-gate/05-DOSSIER-SCHEMA.json, tools/quality-gate/proof-v2-probe/src/main.rs, tools/quality-gate/lifetime-probe/src/main.rs, tools/quality-gate/tag-probe/src/main.rs, tools/quality-gate/safe-aad-probe/src/main.rs</read_first>
  <action>First normalize the four dossier files to clearly marked templates with evidence values empty, then commit all probe, fixture, parity-test, validator, CI-workflow, and dossier-template source changes; because this plan depends on 05-01, that commit necessarily includes the final CI workflow. Record it as `tested_source_sha` with its two gitlink SHAs and SHA-256 of `pnpm-lock.yaml`. Create a detached temporary worktree at tested_source_sha. In that worktree record `node --version` and `CI=true npx --yes pnpm@10.18.3 --version`, hash the lockfile, run `CI=true npx --yes pnpm@10.18.3 install --frozen-lockfile`, and assert the post-install lockfile hash equals the recorded pre-install hash before any build or Vitest command. Then run compile/build, all four exact cargo generators and fixture diffs, focused TS suites, and negative controls using `CI=true npx --yes pnpm@10.18.3`. Fill dossier attestations afterward in the main worktree, all naming tested_source_sha and captured outputs. Commit evidence separately; record that commit as `attestation_sha` in the plan summary and later final gate, outside the attestation commit itself. Do not iterate in pursuit of a document containing its own commit hash, and never claim the attestation commit is the tested source.</action>
  <verify><automated>test "$(git show "$(rg -o 'tested_source_sha: [0-9a-f]{40}' .planning/phases/05-quality-gate/05-PROOF-V2-DOSSIER.md | awk '{print $2}')":pnpm-lock.yaml | sha256sum | awk '{print $1}')" = "$(rg -o 'lockfile_sha256: [0-9a-f]{64}' .planning/phases/05-quality-gate/05-PROOF-V2-DOSSIER.md | awk '{print $2}')" &amp;&amp; node scripts/validate-quality-dossiers.mjs &amp;&amp; CI=true npx --yes pnpm@10.18.3 vitest run src/__tests__/conformance/proof-v2-parity.test.ts src/core/__tests__/key-package-lifetime-parity.test.ts src/core/__tests__/key-package-tag-parity.test.ts src/core/components/__tests__/safe-aad-parity.test.ts</automated></verify>
  <done>All four dossiers attest one immutable tested_source_sha; the later summary records the distinct attestation_sha and the validator confirms completeness and independence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Four independent dossiers → tested-source claim | Mixed commits or incomplete evidence can falsely satisfy QA-02. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-07 | Tampering | dossier evidence | critical | mitigate | Machine-check tuple, fields, negative controls, and oracle independence. |
| T-05-SC | Tampering | package installs | high | mitigate | Standalone probes use committed Cargo.lock files and --locked. |
</threat_model>
<verification>Schema self-test, all generators, fixture diffs, focused tests, and final validator pass.</verification>
<acceptance_criteria>QA-02 evidence is complete and independently generated for one immutable tested_source_sha, with later attestation provenance explicit.</acceptance_criteria>
<success_criteria>No dossier can pass while stale, incomplete, self-referential, or bound to a different SHA tuple.</success_criteria>
<output>Create `05-06-SUMMARY.md` when done.</output>
