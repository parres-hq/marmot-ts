---
phase: 05-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/tests.yml
  - .planning/phases/05-quality-gate/05-RUNTIME-EVIDENCE.md
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "Per D-01, every Node 20/22/24, Deno 2, Bun latest/1.1 CI job runs both normal and isolated extended suites."
    - "Per D-02, local Node 22, Deno 2, and Bun latest results are labeled smoke evidence only."
    - "Per D-03, CI installs pnpm 10 and dependencies with --frozen-lockfile."
  artifacts:
    - ".github/workflows/tests.yml"
    - ".planning/phases/05-quality-gate/05-RUNTIME-EVIDENCE.md"
  key_links:
    - "Each matrix job invokes its runtime's normal command and the same runtime's --config vitest.extended.config.ts command."
---

<objective>
Make the six-runtime release matrix exercise both suite classes and capture honest locally available smoke results.

Purpose: Establish authoritative QA-01 evidence without treating unavailable local versions as tested.
Output: Updated CI workflow and revision-bound runtime evidence.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-quality-gate/05-CONTEXT.md
@.planning/phases/05-quality-gate/05-RESEARCH.md
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Run normal and extended suites in every runtime job</name>
  <files>.github/workflows/tests.yml</files>
  <read_first>.github/workflows/tests.yml, vitest.config.ts, vitest.extended.config.ts, package.json</read_first>
  <action>Per D-01, add a distinct extended-suite step to all three matrix job definitions: Node uses `pnpm conformance:extended`; Deno uses `deno run -A --node-modules-dir=auto npm:vitest run --config vitest.extended.config.ts`; Bun uses `bun run vitest run --config vitest.extended.config.ts`. Keep the existing normal commands. Per D-03, retain `pnpm/action-setup@v4` version 10 and `pnpm install --frozen-lockfile`. Add an explicit version-reporting step in every matrix job: Node runs `node --version` and `pnpm --version`; Deno runs `deno --version` and `pnpm --version`; Bun runs `bun --version`, `bun --revision`, and `pnpm --version`. Do not rely on setup-action labels as runtime evidence.</action>
  <verify><automated>node -e 'const fs=require("fs");const s=fs.readFileSync(".github/workflows/tests.yml","utf8");for(const x of ["20.x", "22.x", "24.x", "v2.x", "latest", "1.1", "version: 10", "pnpm install --frozen-lockfile", "vitest.extended.config.ts", "node --version", "deno --version", "bun --version", "bun --revision"])if(!s.includes(x))throw new Error(x);if((s.match(/pnpm --version/g)||[]).length!==3)throw new Error("every matrix job must print pnpm version")'</automated></verify>
  <done>All six declared runtime rows execute normal plus extended Vitest, while pnpm 10 and frozen installation remain explicit per D-01/D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Capture locally available smoke evidence</name>
  <files>.planning/phases/05-quality-gate/05-RUNTIME-EVIDENCE.md</files>
  <read_first>AGENTS.md, .planning/phases/05-quality-gate/05-RESEARCH.md, .github/workflows/tests.yml</read_first>
  <action>Run and record exact versions, UTC timestamp, repository/spec/MDK SHAs, commands, exit codes, counts, and durations for local Node 22 (`pnpm vitest run`; `pnpm conformance:extended`), Deno 2 (normal and extended runtime-specific commands), and Bun latest (normal and extended runtime-specific commands). Per D-02, label every local row SMOKE and explicitly mark Node 20, Node 24, and Bun 1.1 unavailable locally/CI-required; never infer their results. Record local pnpm's actual version and do not describe it as pnpm 10. Also run `pnpm compile`, `pnpm build`, and `pnpm lint` as separate local gates.</action>
  <verify><automated>pnpm compile &amp;&amp; pnpm vitest run &amp;&amp; pnpm conformance:extended &amp;&amp; deno run -A --node-modules-dir=auto npm:vitest run &amp;&amp; deno run -A --node-modules-dir=auto npm:vitest run --config vitest.extended.config.ts &amp;&amp; bun run vitest run &amp;&amp; bun run vitest run --config vitest.extended.config.ts</automated></verify>
  <done>The evidence names only installed local versions as smoke passes and reserves complete six-runtime authority for CI per D-02.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CI configuration → release claim | Version aliases and omitted suites can create false release evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering | tests.yml | high | mitigate | Preserve pinned pnpm/frozen install and run both configs in every job. |
| T-05-02 | Repudiation | runtime evidence | high | mitigate | Record immutable SHAs, versions, commands, exits, counts, and URLs. |
| T-05-SC | Tampering | package installs | high | mitigate | No new packages; existing lockfile remains frozen. |
</threat_model>

<verification>Workflow contains six runtime versions and runtime-correct normal/extended commands; all locally installed runtime smoke commands exit 0.</verification>
<acceptance_criteria>QA-01 has a complete CI execution contract and truthful local smoke record implementing D-01, D-02, and D-03.</acceptance_criteria>
<success_criteria>Every supported runtime is covered in CI by both suites; no unavailable local runtime is claimed as run.</success_criteria>
<output>Create `.planning/phases/05-quality-gate/05-01-SUMMARY.md` when done.</output>
