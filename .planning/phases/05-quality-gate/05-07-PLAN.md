---
phase: 05-quality-gate
plan: 07
type: execute
wave: 3
depends_on: [05-01, 05-06]
files_modified: []
autonomous: false
requirements: [QA-01, QA-02]
must_haves:
  truths: ["The exact locally validated tested_source_sha is pushed before authoritative hosted CI is requested."]
  artifacts: []
  key_links: ["User push authorization exposes tested_source_sha to GitHub Actions without the executor assuming external mutation authority."]
---
<objective>Establish the explicit external-state boundary required before authoritative CI.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/05-quality-gate/05-RUNTIME-EVIDENCE.md
@.planning/phases/05-quality-gate/05-06-SUMMARY.md</context>
<tasks>
<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Push the immutable tested source and authenticate GitHub CLI</name>
  <action>Pause execution before any push or hosted-run assumption. Read `tested_source_sha` and `attestation_sha` from 05-06-SUMMARY.md and require the user resume signal to name tested_source_sha.</action>
  <instructions>Push the exact tested_source_sha (it may be an ancestor of current HEAD because dossier/summary attestation commits came later) to a remote ref that triggers Tests, ensure that commit is visible on GitHub, run `gh auth status`, and trigger or confirm the Tests workflow for tested_source_sha. Do not substitute current HEAD or attestation_sha.</instructions>
  <verification>After resume, require the supplied SHA to equal the recorded tested_source_sha, verify it with `gh api repos/{owner}/{repo}/commits/{tested_source_sha}`, and retain the distinct attestation_sha for Plan 08.</verification>
  <what-built>The executor has completed local runtime smoke, all four parity dossiers, and dossier validation for tested_source_sha.</what-built>
  <how-to-verify>Confirm the tested_source_sha from 05-06-SUMMARY.md is reachable on GitHub and is the workflow head SHA. Confirm the separately recorded attestation_sha exists locally/remotely as evidence provenance. Authenticate `gh`; do not require current HEAD to equal tested_source_sha.</how-to-verify>
  <resume-signal>Reply `pushed &lt;40-hex-tested_source_sha&gt;` after that exact source commit is remote and GitHub CLI is authenticated.</resume-signal>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Local git → GitHub | Push and authentication are external state changes requiring explicit user action/authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-08 | Spoofing | remote tested source | critical | mitigate | Require exact 40-hex tested_source_sha and authenticated GitHub visibility before retrieval. |
| T-05-SC | Tampering | package installs | high | accept | No package operation occurs in this checkpoint. |
</threat_model>
<verification>The resumed SHA equals recorded tested_source_sha and authenticated GitHub resolves it; attestation_sha is recorded separately.</verification>
<acceptance_criteria>No workflow evidence is requested or trusted until the user confirms tested_source_sha was pushed.</acceptance_criteria>
<success_criteria>The immutable tested source exists remotely and GitHub CLI authentication is ready.</success_criteria>
<output>Create `05-07-SUMMARY.md` when done.</output>
