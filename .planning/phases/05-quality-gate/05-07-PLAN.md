---
phase: 05-quality-gate
plan: 07
type: execute
wave: 3
depends_on: [05-01, 05-06]
files_modified: []
autonomous: false
requirements: [QA-01, QA-02]
must_haves:
  truths: ["The exact locally validated candidate SHA is pushed before authoritative hosted CI is requested."]
  artifacts: []
  key_links: ["User push authorization exposes the immutable candidate commit to GitHub Actions without the executor assuming external mutation authority."]
---
<objective>Establish the explicit external-state boundary required before authoritative CI.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/05-quality-gate/05-RUNTIME-EVIDENCE.md
@.planning/phases/05-quality-gate/05-06-SUMMARY.md</context>
<tasks>
<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Push the immutable candidate and authenticate GitHub CLI</name>
  <action>Pause execution before any push or hosted-run assumption. Require the user-provided resume signal containing the exact candidate SHA.</action>
  <instructions>Confirm `git status --short` is empty and note `git rev-parse HEAD`. Push that exact SHA to the remote branch (or explicitly authorize the executor to push), ensure it is visible on GitHub, run `gh auth status`, and trigger or confirm the Tests workflow for that commit. Do not amend or add commits afterward.</instructions>
  <verification>After resume, require the supplied 40-hex SHA to equal local HEAD and verify it resolves with authenticated `gh api repos/{owner}/{repo}/commits/{sha}` before Plan 08 starts.</verification>
  <what-built>The executor has completed local runtime smoke, all four parity dossiers, and dossier validation at the candidate SHA.</what-built>
  <how-to-verify>Confirm `git status --short` is empty and note `git rev-parse HEAD`. Push that exact SHA to the remote branch (or authorize the executor to push if desired), ensure it is visible on GitHub, run `gh auth status`, and trigger/confirm the Tests workflow for that commit. Do not amend or add commits after noting the SHA.</how-to-verify>
  <resume-signal>Reply `pushed &lt;40-hex-candidate-sha&gt;` after the exact commit is remote and GitHub CLI is authenticated.</resume-signal>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Local git → GitHub | Push and authentication are external state changes requiring explicit user action/authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-08 | Spoofing | remote candidate | critical | mitigate | Require exact 40-hex SHA and authenticated GitHub visibility before retrieval. |
| T-05-SC | Tampering | package installs | high | accept | No package operation occurs in this checkpoint. |
</threat_model>
<verification>The resumed SHA equals local HEAD and `gh api repos/{owner}/{repo}/commits/{sha}` resolves.</verification>
<acceptance_criteria>No workflow evidence is requested or trusted until the user confirms the exact candidate was pushed.</acceptance_criteria>
<success_criteria>The immutable candidate exists remotely and GitHub CLI authentication is ready.</success_criteria>
<output>Create `05-07-SUMMARY.md` when done.</output>
