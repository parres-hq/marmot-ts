---
phase: 04.1-terminal-group-disbanding
plan: 03
type: execute
wave: 3
depends_on: [04.1-02]
files_modified:
  - src/client/marmot-client.ts
  - src/client/groups-manager.ts
  - src/client/group-factory.ts
  - src/client/group-registry.ts
  - src/client/group/marmot-group.ts
  - src/client/group/__tests__/marmot-group.test.ts
  - src/client/__tests__/marmot-client.test.ts
  - src/__tests__/groups-manager.test.ts
requirements: [LIFE-01, LIFE-02]
autonomous: true
must_haves:
  truths:
    - "LIFE-02/D-01: every created, joined, imported, or loaded group receives the same durable lifecycle store and hydrates `${groupId}/disband/request` before use"
    - "LIFE-01: legacy groups expose an atomic enableDisbanding operation that publishes active state plus required id only when every resulting leaf supports 0x800c"
    - "LIFE-02/D-03: MarmotGroup.disband persists intent before publish, confirms on acknowledgement, and preserves the request for regeneration after failure or an active winner"
  artifacts:
    - path: src/client/marmot-client.ts
      provides: "Durable lifecycle-store resolution at the composition root"
    - path: src/client/group/marmot-group.ts
      provides: "Public enableDisbanding and disband facades"
    - path: src/__tests__/groups-manager.test.ts
      provides: "Construction-path coverage for create, join/import, and load"
  key_links:
    - from: src/client/marmot-client.ts
      to: src/client/group/marmot-group.ts
      via: "one lifecycleStore threaded through GroupsManager, GroupFactory or GroupRegistry, and group options"
      pattern: "lifecycleStore"
    - from: src/client/group/marmot-group.ts
      to: src/client/runtime/group-runtime.ts
      via: "publish, acknowledgement, confirmation, and rollback lifecycle"
      pattern: "(enableDisbanding|disband)"
---

<objective>
Expose lifecycle enablement and durable disband request publication through the public group facade while proving every client construction path carries the same store.

Purpose: Turn the validated low-level request from Plan 02 into a restart-safe downstream API without hiding persistence in an ephemeral store.
Output: Client/manager/factory/registry store wiring, public group methods, and publication lifecycle tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-terminal-group-disbanding/04.1-CONTEXT.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-RESEARCH.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-PATTERNS.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-02-SUMMARY.md
@refs/marmot/app-components/group-lifecycle-v1.md
@refs/mdk/crates/cgka-engine/src/disband.rs

<interfaces>
- Plan 02 creates `DisbandRequestStore` and adds its explicit dependency to `GroupSessionOptions`/engine construction.
- `MarmotClient` is the composition root; `GroupsManager` owns `GroupFactory` for create/import and `GroupRegistry` for load/join paths.
- `GroupRuntime` already owns publish-before-apply effects: wrap, publish, acknowledge/confirm, and rollback before acknowledgement.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread one durable lifecycle store through every group construction path</name>
  <read_first>src/client/marmot-client.ts, src/client/groups-manager.ts, src/client/group-factory.ts, src/client/group-registry.ts, src/client/group/marmot-group.ts, src/client/__tests__/marmot-client.test.ts, src/__tests__/groups-manager.test.ts</read_first>
  <files>src/client/marmot-client.ts, src/client/groups-manager.ts, src/client/group-factory.ts, src/client/group-registry.ts, src/client/group/marmot-group.ts, src/client/__tests__/marmot-client.test.ts, src/__tests__/groups-manager.test.ts</files>
  <behavior>
    - Caller-supplied lifecycleStore is passed by identity to created, imported/joined, and loaded groups.
    - When omitted, MarmotClient creates a group-id-namespaced adapter over durable groupStateStore, never an in-memory fallback.
    - Pending request hydration completes before activation admits sends, routing, or convergence scheduling.
  </behavior>
  <action>Add `lifecycleStore?: GenericKeyValueStore&lt;Uint8Array&gt;` to `MarmotClientOptions`; resolve it once to the supplied store or a namespaced adapter over durable `groupStateStore`. Thread that exact instance through `GroupsManagerOptions`, `GroupFactoryOptions`, `GroupRegistryOptions`, and `MarmotGroupOptions` into Plan 02's `GroupSessionOptions` dependency for create, join, import, and load. Pending intent uses `${groupId}/disband/request`; Plan 05 must reuse this same instance for `${groupId}/disband/terminal`, not introduce a second store. Cover factory create/import, manager join, registry restart load, and concurrent load identity in manager/client tests. Do not fall back to `InMemoryKeyValueStore` for lifecycle state.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/client/__tests__/marmot-client.test.ts src/__tests__/groups-manager.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>All construction paths receive one durable store by identity, omitted configuration remains durable via namespacing, Pending hydration precedes activation, and compile passes.</acceptance_criteria>
  <done>Restart-safe request state reaches every public group instance before it can do work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish legacy enablement and disband through MarmotGroup</name>
  <read_first>refs/marmot/app-components/group-lifecycle-v1.md, src/client/group/marmot-group.ts, src/client/runtime/group-runtime.ts, src/client/session/group-effects.ts, src/client/group/__tests__/marmot-group.test.ts</read_first>
  <files>src/client/group/marmot-group.ts, src/client/group/__tests__/marmot-group.test.ts</files>
  <behavior>
    - enableDisbanding publishes and confirms one atomic active-state plus required-id Commit; unsupported members or legality failure publish nothing and leave state unchanged.
    - disband persists intent before preparation, publishes once through GroupRuntime, confirms on relay acknowledgement, and rolls back only staged publication on pre-ack failure.
    - Acknowledged disband retains request/candidate state until canonical selection; repeated calls return the existing request instead of parallel publication.
    - An active canonical winner leaves Pending for Plan 04 regeneration; selected disband clears it during Plan 05 terminal settlement.
  </behavior>
  <action>Expose named public `MarmotGroup.enableDisbanding()` and `MarmotGroup.disband()` methods over the Plan 02 session intents. Route both through existing `GroupRuntime` publish-before-apply behavior: wrap before PendingPublish, publish once, confirm only after acknowledgement, roll back staged state on pre-ack failure, and never erase the durable request/candidate merely because publication or selection did not terminalize. Return typed results for unsupported members, legality/authority loss, pending idempotency, publish failure, and acknowledged request. Keep regeneration scheduler-owned rather than recursively publishing from the facade.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/client/group/__tests__/marmot-group.test.ts src/client/session/__tests__/group-session.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Public tests prove atomic enablement, support gating, persist-before-publish, acknowledgement, rollback, idempotency, and retained regeneration intent.</acceptance_criteria>
  <done>Downstream callers can safely enable legacy lifecycle support and request durable group disbanding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| client configuration → group construction | A missing/ephemeral store could lose irreversible intent on restart. |
| public group call → relay acknowledgement | Local intent must remain distinct from staged and confirmed publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-17 | Tampering | lifecycle-store composition | high | mitigate | One durable resolved store, identity tests across every construction path, and no ephemeral fallback. |
| T-04.1-18 | Elevation of privilege | legacy enablement facade | critical | mitigate | Reuse shared admin/capability legality and publish nothing on failure. |
| T-04.1-19 | Repudiation | disband publish lifecycle | high | mitigate | Persist request first and test typed acknowledgement/rollback outcomes. |
</threat_model>

<verification>Run pinned focused client, manager, group, and session tests plus compile; Phase 5 owns the runtime matrix.</verification>
<success_criteria>Public APIs are durable, capability-gated, publish-before-apply, idempotent, and available on every constructed group.</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Publicly initiate lifecycle enablement/disband | 04.1-03 | COVERED | Durable facade over Plans 01-02. |
| REQ | LIFE-01 | Atomic legacy enablement public path | 04.1-03 | COVERED | Uses Plan 02 legality. |
| REQ | LIFE-02 | Durable public request and publish lifecycle | 04.1-03 | COVERED | Includes all construction paths. |
| RESEARCH | — | Explicit lifecycle store and regeneration-safe acknowledgement | 04.1-03 | COVERED | Request and terminal namespaces share one durable backend. |
| CONTEXT | D-01/D-03 | Durable intent and typed outbound behavior | 04.1-03 | COVERED | User-confirmed persistence choice. |

<output>Create `.planning/phases/04.1-terminal-group-disbanding/04.1-03-SUMMARY.md` when done.</output>
