---
phase: 04.1-terminal-group-disbanding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/components/group-lifecycle.ts
  - src/core/components/ids.ts
  - src/core/components/dictionary.ts
  - src/core/components/index.ts
  - src/core/client-state.ts
  - src/core/components/__tests__/group-lifecycle.test.ts
  - src/core/components/__tests__/dictionary.test.ts
requirements: [LIFE-01]
autonomous: true
must_haves:
  truths:
    - "LIFE-01: new groups advertise and require marmot.group.lifecycle.v1 in active state while legacy groups without it remain readable"
    - "LIFE-01: lifecycle bytes accept exactly 00 and 01 and reject empty, unknown, or trailing input"
    - "LIFE-01/D-02: callers can inspect the decoded lifecycle component through the established public group view"
  artifacts:
    - path: src/core/components/group-lifecycle.ts
      provides: "Exact lifecycle component codec and named literal-union contract"
      exports: [groupProtocolLifecycleValues, GroupProtocolLifecycleValue, encodeGroupLifecycleV1, decodeGroupLifecycleV1]
    - path: src/core/components/dictionary.ts
      provides: "Typed lifecycle dictionary descriptor, getter, and entry builder"
    - path: src/core/client-state.ts
      provides: "Lifecycle projection on MarmotGroupView and component diagnostics"
  key_links:
    - from: src/core/components/dictionary.ts
      to: src/core/components/group-lifecycle.ts
      via: "registered 0x800c codec"
      pattern: "GROUP_LIFECYCLE_COMPONENT_ID"
    - from: src/core/client-state.ts
      to: src/core/components/dictionary.ts
      via: "typed lifecycle getter"
      pattern: "getGroupLifecycle"
---

<objective>
Make the lifecycle component a byte-exact, default-enabled, publicly inspectable capability without breaking legacy group state.

Purpose: Establish the LIFE-01 wire and projection contract before disband legality or convergence can consume it.
Output: Component codec, dictionary registration/defaults, view projection, and exact automated vectors.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-CONTEXT.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-RESEARCH.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-PATTERNS.md
@refs/marmot/app-components/group-lifecycle-v1.md
@refs/marmot/protocol-core/group-setup.md
@refs/mdk/crates/cgka-engine/src/app_components.rs

<interfaces>
Existing contracts to extend directly:
- `DEFAULT_GROUP_COMPONENT_IDS` and `SUPPORTED_APP_COMPONENT_IDS` in `src/core/components/ids.ts` are the authoritative new-group and LeafNode capability lists.
- `buildAppDataDictionary`, `makeLeafAppComponentsExtension`, typed `get*` accessors, and `*Entry` builders live in `src/core/components/dictionary.ts`.
- `MarmotGroupView` and `getMarmotGroupView(ClientState)` live in `src/core/client-state.ts`; malformed known components make the view null and populate `decodeError` in detailed info.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry active lifecycle state from new-group bytes to the public view</name>
  <read_first>refs/marmot/app-components/group-lifecycle-v1.md, refs/marmot/protocol-core/group-setup.md, src/core/components/ids.ts, src/core/components/dictionary.ts, src/core/client-state.ts, src/core/components/__tests__/dictionary.test.ts</read_first>
  <files>src/core/components/group-lifecycle.ts, src/core/components/ids.ts, src/core/components/dictionary.ts, src/core/client-state.ts, src/core/components/__tests__/dictionary.test.ts</files>
  <behavior>
    - New group dictionary state contains component 0x800c encoded as 00 and the required list includes 0x800c.
    - Leaf capability advertisement includes 0x800c.
    - The public group view decodes the component as active; legacy state with neither lifecycle state nor required id remains readable.
  </behavior>
  <action>Create the named enum-like `groupProtocolLifecycleValues` contract and exact Uint8Array codec for `marmot.group.lifecycle.v1`; register `GROUP_LIFECYCLE_COMPONENT_ID = 0x800c` and its name, include it in both supported capabilities and new-group defaults, expose a typed dictionary getter/entry builder, and project it through `MarmotGroupView`. Follow LIFE-01 and D-02, NodeNext `.js` imports, named exports, and the existing dictionary diagnostic behavior. Start with the failing end-to-end dictionary-to-view assertion, then implement the production path. Update the authoritative LeafNode dictionary byte vector to match the new component ordering and bytes; cite topic-organized spec paths.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/core/components/__tests__/dictionary.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>The failing tracer proves a newly created state carries active lifecycle bytes through the dictionary into `MarmotGroupView`; legacy omission still decodes; focused tests and strict compile pass.</acceptance_criteria>
  <done>A real caller can create a default group and observe lifecycle active through the public view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin exact lifecycle bytes and fail-closed decoding</name>
  <read_first>refs/marmot/app-components/group-lifecycle-v1.md, src/core/components/message-retention.ts, src/core/components/__tests__/components.test.ts, src/core/components/__tests__/dictionary.test.ts</read_first>
  <files>src/core/components/group-lifecycle.ts, src/core/components/__tests__/group-lifecycle.test.ts, src/core/components/__tests__/dictionary.test.ts</files>
  <behavior>
    - active encodes as 00 and disbanded encodes as 01, each round-tripping exactly.
    - empty input, 02, and either valid byte followed by trailing data throw.
    - an absent legacy component returns undefined while malformed present data follows existing fail-closed view/info behavior.
  </behavior>
  <action>Export the lifecycle codec through the component barrel and add table-driven byte tests modeled on the existing one-byte component codecs, including exact-length enforcement through `BinaryReader.end()`. Verify the dictionary getter and entry builder preserve exact bytes and absence semantics. Do not introduce TypeScript enums, Buffer, runtime-specific APIs, permissive trailing-byte parsing, or a new dependency.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/core/components/__tests__/group-lifecycle.test.ts src/core/components/__tests__/dictionary.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>All exact and rejection vectors pass, malformed known lifecycle data fails closed, and the strict library build passes.</acceptance_criteria>
  <done>LIFE-01 has an executable byte contract and backward-compatible component projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted/network component bytes → typed lifecycle | Untrusted bytes choose an absorbing protocol meaning. |
| default dictionary → peer capability negotiation | Emitted ids and bytes become interoperability-visible state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-01 | Tampering | lifecycle codec | high | mitigate | Exact one-byte decoder rejects unknown and trailing data under vectors. |
| T-04.1-02 | Denial of service | legacy group load | medium | mitigate | Treat absent component as valid legacy state and isolate malformed-present decoding. |
| T-04.1-03 | Spoofing | capability advertisement | medium | mitigate | One authoritative id list drives LeafNode and new-group defaults with exact-byte regression. |
</threat_model>

<verification>Run both focused component suites and `pnpm compile`; do not run Phase 5's Node/Deno/Bun matrix.</verification>
<success_criteria>Lifecycle bytes, default enablement, legacy compatibility, and public projection are deterministic and tested.</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Implement lifecycle component | 04.1-01 | COVERED | Exact codec and active projection. |
| REQ | LIFE-01 | Component/default/legacy portion | 04.1-01 | COVERED | Commit legality continues in 04.1-02. |
| RESEARCH | — | Exact bytes and new/legacy group behavior | 04.1-01 | COVERED | Follows canonical spec and MDK registry. |
| CONTEXT | D-02 | Public status/view exposure | 04.1-01 | COVERED | View gains decoded protocol lifecycle. |

<output>Create `.planning/phases/04.1-terminal-group-disbanding/04.1-01-SUMMARY.md` when done.</output>
