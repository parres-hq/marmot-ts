---
phase: 04.1-terminal-group-disbanding
plan: 04
type: execute
wave: 4
depends_on: [04.1-03]
files_modified:
  - src/core/group-lifecycle.ts
  - src/core/__tests__/group-lifecycle.test.ts
  - src/engine/types.ts
  - src/engine/ingest.ts
  - src/engine/group-engine.ts
  - src/engine/fork-recovery.ts
  - src/engine/__tests__/convergence-scheduling.test.ts
  - src/engine/__tests__/disband-convergence.test.ts
requirements: [CONV-05, LIFE-02]
autonomous: true
must_haves:
  truths:
    - "CONV-05/D-05: every valid disband candidate, including a lone linear edge, enters bounded Recovering and canonical branch selection"
    - "CONV-05/D-06: forced admission preserves pass generation, opened time, deadline, base epoch, and ordinary branch scoring"
    - "LIFE-02/D-07: only a selected disband branch enters absorbing Disbanded; an active winner returns Stable and leaves the durable request pending for regeneration"
  artifacts:
    - path: src/core/group-lifecycle.ts
      provides: "Absorbing Disbanded protocol lifecycle and gate matrix"
    - path: src/engine/ingest.ts
      provides: "Validated disband candidate routing around direct linear apply"
    - path: src/engine/fork-recovery.ts
      provides: "Linear-candidate selection and post-selection terminal outcome"
  key_links:
    - from: src/engine/ingest.ts
      to: src/engine/group-engine.ts
      via: "validDisband admission signal after complete legality checks"
      pattern: "validDisband"
    - from: src/engine/fork-recovery.ts
      to: src/core/group-lifecycle.ts
      via: "winner outcome selects Stable or Disbanded transition"
      pattern: "Disbanded"
---

<objective>
Route disband candidates through the unchanged bounded convergence decision, even when no fork exists, and terminalize only the selected result.

Purpose: Enforce the canonical selection boundary without granting disband special priority or mutating pass identity.
Output: Absorbing FSM state, forced admission, linear candidate resolution, candidate metadata, and deterministic scheduler/selection tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-terminal-group-disbanding/04.1-CONTEXT.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-RESEARCH.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-PATTERNS.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-03-SUMMARY.md
@.planning/phases/04-feature-parity-conformance-vectors/04-05-SUMMARY.md
@refs/marmot/protocol-core/convergence.md
@refs/marmot/app-components/group-lifecycle-v1.md
@refs/mdk/crates/cgka-engine/src/distributed_convergence.rs
@refs/mdk/crates/cgka-engine/src/openmls_projection.rs
@src/engine/group-engine.ts
@src/engine/fork-recovery.ts
@src/engine/ingest.ts

<interfaces>
- `groupLifecycleStates`, `transitionLifecycle`, `mayPrepareLocalCommit`, `mayApplyRetainedInbound`, and `mayRunForkDetection` define the protocol FSM and safety gates.
- Phase 4's convergence pass stores immutable `generation`, `openedAtMs`, `deadlineMs`, and base-epoch identity; ordinary selection-relevant input may refresh quiescence only.
- `ForkRecovery` and `selectCanonicalBranch` own branch comparison. Candidate terminal metadata is an outcome carried beside the branch, never a score input.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Force validated linear disband candidates into one immutable pass</name>
  <read_first>refs/marmot/app-components/group-lifecycle-v1.md, refs/marmot/protocol-core/convergence.md, src/core/group-lifecycle.ts, src/core/__tests__/group-lifecycle.test.ts, src/engine/ingest.ts, src/engine/group-engine.ts, src/engine/__tests__/convergence-scheduling.test.ts</read_first>
  <files>src/core/group-lifecycle.ts, src/core/__tests__/group-lifecycle.test.ts, src/engine/types.ts, src/engine/ingest.ts, src/engine/group-engine.ts, src/engine/__tests__/convergence-scheduling.test.ts</files>
  <behavior>
    - Disbanded is reachable only from Recovering and cannot transition out; every outbound/apply/fork gate is false there.
    - A valid disband candidate whose parent is the current tip bypasses ordinary direct-linear apply, is retained with exact commit digest/actor/source metadata, and forces Stable to Recovering.
    - First candidate opens one pass; a candidate joining an open pass preserves generation, openedAtMs, deadlineMs, and base epoch, and duplicate input does not refresh quiescence.
    - Invalid/stale/out-of-horizon candidates do not open or alter a pass.
  </behavior>
  <action>Add the sole `Recovering -> Disbanded` FSM edge and keep Disbanded out of all mutation gates. After MLS authentication and the shared LIFE-01 legality decision, annotate retained candidate evidence with exact Commit identity, actor, source epoch/parent, and terminal outcome. Route a valid linear disband around the ordinary `setState` processed path and explicitly open/join the existing bounded pass per D-05/D-06. Preserve every immutable pass field and refresh quiescence only for genuinely new selection-relevant evidence. Do not infer terminality from Nostr tags, apply it on admission, or create a second timer.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/core/__tests__/group-lifecycle.test.ts src/engine/__tests__/convergence-scheduling.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>FSM and fake-clock tests prove the linear exception enters Recovering exactly once while all pass identity fields remain unchanged and invalid/duplicate evidence has no scheduler effect.</acceptance_criteria>
  <done>Every eligible disband candidate reaches the same bounded selection boundary without premature state mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Select active or disband outcome without changing branch comparison</name>
  <read_first>refs/marmot/protocol-core/convergence.md, refs/mdk/crates/cgka-engine/src/distributed_convergence.rs, src/engine/fork-recovery.ts, src/engine/__tests__/convergence-parity.test.ts, src/engine/__tests__/convergence-scheduling.test.ts</read_first>
  <files>src/engine/fork-recovery.ts, src/engine/group-engine.ts, src/engine/ingest.ts, src/engine/types.ts, src/engine/__tests__/disband-convergence.test.ts</files>
  <behavior>
    - A one-candidate linear disband pass is selectable and a selected disband outcome enters Disbanded.
    - In a competing active/disband fork, existing score and tie-break rules alone select the winner; changing only the terminal marker cannot boost a branch.
    - Active winner returns Stable, clears losing candidate evidence, preserves the local request, and schedules regeneration against selected state.
    - Losing or withdrawn disband candidate produces no terminal event/state; selected disband suppresses ordinary removal/admin/component presentation effects for that Commit.
  </behavior>
  <action>Teach recovery to represent and resolve the valid one-edge disband exception without weakening ordinary empty-branch checks. Carry terminal outcome as post-selection metadata and call the existing comparator unchanged per D-06. At settlement, return Stable for an active winner and signal request regeneration; enter absorbing Disbanded only when selected state plus authenticated candidate evidence agree per D-07. Suppress ordinary state-presentation rows for the selected terminal Commit and retain actor/digest for the persistence plan. Add real-MLS fork tests with in-memory stores and mock networking for both winner directions.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/engine/__tests__/disband-convergence.test.ts src/engine/__tests__/convergence-parity.test.ts src/engine/__tests__/convergence-scheduling.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Linear, active-winner, disband-winner, losing, withdrawal, and score-neutral cases pass against the production comparator and strict compile succeeds.</acceptance_criteria>
  <done>Canonical selection alone decides whether the group resumes active operation or becomes terminal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| valid candidate → convergence scheduler | Terminal candidates must not manipulate pass lifetime or eligibility. |
| frozen branch set → canonical lifecycle | Only selected authenticated evidence may produce irreversible death. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-08 | Denial of service | forced pass admission | high | mitigate | One immutable bounded pass, duplicate suppression, and exact timer/base assertions. |
| T-04.1-09 | Elevation of privilege | canonical terminalization | critical | mitigate | Require shared legality result plus selected authenticated candidate evidence. |
| T-04.1-10 | Tampering | branch comparator | critical | mitigate | Leave comparator input/scoring unchanged and test marker-only score neutrality. |
| T-04.1-11 | Repudiation | terminal actor attribution | high | mitigate | Carry authenticated actor and exact Commit digest from admission to settlement. |
</threat_model>

<verification>Run focused FSM, scheduler, recovery, and real-MLS disband tests plus strict compile; do not claim Phase 5 matrix evidence.</verification>
<success_criteria>Valid disband is a bounded selection exception, not a direct apply or scoring exception.</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Forced Stable to Recovering admission | 04.1-04 | COVERED | Includes lone linear edge. |
| REQ | CONV-05 | Fixed pass and unchanged scoring | 04.1-04 | COVERED | Scheduler and comparator assertions. |
| REQ | LIFE-02 | Canonical-only absorbing outcome | 04.1-04 | COVERED | Persistence follows in 04.1-05. |
| RESEARCH | — | Candidate metadata, linear exception, active regeneration | 04.1-04 | COVERED | Matches MDK seam responsibilities. |
| CONTEXT | D-05/D-06/D-07 | Admission, pass identity, selection boundary | 04.1-04 | COVERED | Each has explicit behavior tests. |

<output>Create `.planning/phases/04.1-terminal-group-disbanding/04.1-04-SUMMARY.md` when done.</output>
