---
phase: 04.1-terminal-group-disbanding
plan: 02
type: execute
wave: 2
depends_on: [04.1-01]
files_modified:
  - src/core/components/disband-validation.ts
  - src/core/components/integrity.ts
  - src/core/components/index.ts
  - src/core/components/__tests__/disband-validation.test.ts
  - src/engine/disband-request.ts
  - src/engine/group-engine.ts
  - src/engine/types.ts
  - src/client/session/group-session.ts
  - src/client/group/marmot-group.ts
  - src/client/group-factory.ts
  - src/client/group-registry.ts
  - src/client/groups-manager.ts
  - src/client/marmot-client.ts
  - src/engine/__tests__/disband-request.test.ts
  - src/client/group/__tests__/marmot-group.test.ts
  - src/client/session/__tests__/group-session.test.ts
requirements: [LIFE-01, LIFE-02]
autonomous: true
must_haves:
  truths:
    - "LIFE-01: send, inbound, and replay share one parent-relative classification of exact disband Commit shape"
    - "LIFE-02: a local disband request is durable, immediately gates ordinary outbound work, and survives publish failure, active-branch selection, and recoverable pauses"
    - "LIFE-02: a pending request regenerates against the selected active state and fails only with typed loss-of-membership or loss-of-admin authority"
  artifacts:
    - path: src/core/components/disband-validation.ts
      provides: "Pure typed disband Commit classifier"
    - path: src/engine/disband-request.ts
      provides: "Versioned durable request codec and request-state helpers"
    - path: src/engine/group-engine.ts
      provides: "Public request/enable APIs and authoritative Disbanding outbound gate"
    - path: src/client/group/marmot-group.ts
      provides: "Public enableDisbanding/disband methods using publish-before-apply"
    - path: src/client/marmot-client.ts
      provides: "One durable request-store resolution and construction-chain injection"
  key_links:
    - from: src/core/components/integrity.ts
      to: src/core/components/disband-validation.ts
      via: "shared validateCommitLegality adapter"
      pattern: "classifyDisbandCommit"
    - from: src/engine/group-engine.ts
      to: src/engine/disband-request.ts
      via: "persist-before-prepare request workflow"
      pattern: "disbandRequest"
    - from: src/client/marmot-client.ts
      to: src/client/session/group-session.ts
      via: "same disbandRequestStore threaded through manager, factory, registry, and group options"
      pattern: "disbandRequestStore"
    - from: src/client/group/marmot-group.ts
      to: src/client/runtime/group-runtime.ts
      via: "enableDisbanding/disband publish, acknowledgement, and rollback lifecycle"
      pattern: "(enableDisbanding|disband)"
---

<objective>
Define one exact disband legality decision and a durable local request that keeps trying safely until canonical resolution.

Purpose: Prevent seam drift and preserve the spec-required irreversible user intent across transport failure or a competing active winner.
Output: Pure validation, typed violations, request persistence/regeneration, public request API, and focused behavior tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-terminal-group-disbanding/04.1-CONTEXT.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-RESEARCH.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-PATTERNS.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-01-SUMMARY.md
@refs/marmot/app-components/group-lifecycle-v1.md
@refs/mdk/crates/cgka-engine/src/disband.rs
@src/core/components/integrity.ts
@src/engine/group-engine.ts
@src/engine/types.ts

<interfaces>
- `validateCommitLegality(parentState, resultingState, proposals)` is the shared non-throwing legality adapter consumed by outbound, inbound, and recovery seams.
- `CommitIntegrityViolationReason` is the protocol-safe literal union; details must not expose raw identity material.
- `MarmotGroupEngine.send(intent)` and its queued-intent path are the authoritative outbound mutation seam.
- Existing durable engine data uses the injected `GenericKeyValueStore&lt;Uint8Array&gt;` rather than runtime storage APIs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify exact parent-relative disband legality at every Commit seam</name>
  <read_first>refs/marmot/app-components/group-lifecycle-v1.md, refs/mdk/crates/cgka-engine/src/disband.rs, src/core/components/integrity.ts, src/core/components/dictionary.ts, src/core/components/__tests__/integrity.test.ts</read_first>
  <files>src/core/components/disband-validation.ts, src/core/components/integrity.ts, src/core/components/index.ts, src/core/components/__tests__/disband-validation.test.ts</files>
  <behavior>
    - Returns validDisband with authenticated actor only for active-required parent to disbanded result, parent admin committer, committer-only resulting admin list, exact-leaf removals, and exactly one inline lifecycle plus admin-policy replacement.
    - A single-leaf group needs no Remove; same-account sibling leaves still must be removed.
    - Legacy/not-enabled groups classify as notDisband unless they attempt an invalid terminal transition; forbidden proposal kinds, by-reference lifecycle, missing/full policy replacement, non-admin, and unrelated updates return a typed violation.
    - Legacy enablement succeeds only as one Commit that adds active lifecycle state and required id 0x800c together after every resulting leaf advertises 0x800c; partial enablement or any unsupported resulting leaf is rejected.
  </behavior>
  <action>Create the pure discriminated `DisbandClassification` and integrate it into `validateCommitLegality`, deriving authorization and exact leaf indices from the authenticated candidate parent rather than the current tip. Preserve proposal order/form and reject every proposal outside the lifecycle spec whitelist. Add the legacy enablement decision to the same shared legality adapter: the Commit must atomically add active lifecycle bytes plus required id 0x800c, must be admin-authored, and must verify capability 0x800c on every resulting leaf before accepting either update; reject state-only, required-id-only, disband-on-enable, removal-after-required, and unsupported-member variants. Extend typed integrity and ingest reasons so outbound throws, inbound rejection, and recovery edge-dropping consume the same decision. Implement LIFE-01 using ts-mls decoded proposals rather than parsing wire bytes again.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/core/components/__tests__/disband-validation.test.ts src/core/components/__tests__/integrity.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>The legality table covers the complete disband predicate plus atomic legacy enablement and all-member capability gate, all three existing legality consumers compile against the shared result, and focused tests pass.</acceptance_criteria>
  <done>No send, inbound, or replay seam can disagree about whether a candidate is a valid disband.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and regenerate irreversible local disband requests</name>
  <read_first>refs/marmot/app-components/group-lifecycle-v1.md, refs/mdk/crates/cgka-engine/src/disband.rs, src/client/session/group-session.ts, src/engine/group-engine.ts, src/utils/key-value.ts, src/client/session/__tests__/group-session.test.ts</read_first>
  <reversibility rating="one-way">D-01 is already user-confirmed in CONTEXT.md; retain the migration warning without adding a redundant decision checkpoint.</reversibility>
  <files>src/engine/disband-request.ts, src/engine/types.ts, src/client/session/group-session.ts, src/engine/group-engine.ts, src/engine/__tests__/disband-request.test.ts, src/client/session/__tests__/group-session.test.ts</files>
  <behavior>
    - Requesting disband persists one Pending request before preparation and repeated requests return the same logical request.
    - Pending request immediately blocks application messages, commits, maintenance, and queued-drain work with a typed disbanding reason.
    - Publish failure, Recovering/Unrecoverable pause, and a selected active branch preserve Pending; active selection clears last-prepared epoch and regenerates against the selected state.
    - Loss of membership or admin authority changes Pending to a typed Failed reason; selected disband clears the request during terminal settlement.
    - A reconstructed session/engine hydrates Pending from an explicit durable request store before admitting outbound or convergence work.
  </behavior>
  <action>Implement LIFE-02 and D-01/D-03 with a versioned `DisbandRequestStore` backed by an explicit `GenericKeyValueStore&lt;Uint8Array&gt;`, injected into GroupSession and MarmotGroupEngine rather than hidden inside the existing ingestion store. Hydrate request state before the engine admits outbound or scheduler work. Persist before preparing the exact inline lifecycle/admin-policy/removal Commit, record last-prepared epoch, and reuse shared legality. Keep a non-canonical Disbanding gate separate from `GroupLifecycleState`; reject ordinary outbound/queued work while unresolved, regenerate after an active winner, and retain through transient publish or repairable convergence states. Only NoLongerMember/NoLongerAdmin may fail the request before terminalization. Follow MDK request/candidate separation and do not cancel because a candidate loses.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/engine/__tests__/disband-request.test.ts src/client/session/__tests__/group-session.test.ts src/engine/__tests__/group-engine.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Low-level construction tests prove the explicit durable request store hydrates before work; crash/restart and publish-failure tests prove request survival; active-winner tests prove regeneration; authority loss is typed; Pending gates all ordinary engine/session outbound seams.</acceptance_criteria>
  <done>A local irreversible disband request is durable and keeps progressing against canonical active state until a defined terminal outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thread the durable store and expose public enable/disband publication</name>
  <read_first>src/client/marmot-client.ts, src/client/groups-manager.ts, src/client/group-factory.ts, src/client/group-registry.ts, src/client/group/marmot-group.ts, src/client/runtime/group-runtime.ts, src/client/group/__tests__/marmot-group.test.ts, src/client/__tests__/marmot-client.test.ts</read_first>
  <files>src/client/marmot-client.ts, src/client/groups-manager.ts, src/client/group-factory.ts, src/client/group-registry.ts, src/client/group/marmot-group.ts, src/client/group/__tests__/marmot-group.test.ts, src/client/__tests__/marmot-client.test.ts</files>
  <behavior>
    - MarmotClient resolves one explicit durable request store to the caller-supplied store or a group-id-namespaced view of durable groupStateStore, never an in-memory fallback, and passes the same store to created, joined, imported, and loaded groups.
    - `MarmotGroup.enableDisbanding()` publishes and confirms the atomic legacy enablement Commit and surfaces unsupported-member/legality failures without changing state.
    - `MarmotGroup.disband()` persists intent before preparing, publishes through GroupRuntime, confirms on relay acknowledgement, rolls back only staged publication on failure, and leaves the durable request pending for canonical regeneration.
    - Repeated disband calls are idempotent and return the existing request/result rather than publishing parallel Commit attempts.
  </behavior>
  <action>Add `disbandRequestStore?: GenericKeyValueStore&lt;Uint8Array&gt;` to `MarmotClientOptions`; resolve it once to the supplied store or a group-id-namespaced adapter over the already durable `groupStateStore`, then thread that exact instance through GroupsManager, GroupFactory, GroupRegistry, MarmotGroupOptions, and GroupSessionOptions for every construction/hydration path. Expose `MarmotGroup.enableDisbanding()` for the Task 1 atomic active+required-id Commit and `MarmotGroup.disband()` for the Task 2 request. Route both through the established GroupSession/GroupRuntime publish-before-apply contract: wrap before PendingPublish, publish once, confirm on acknowledgement, roll back staged state on pre-ack failure, and preserve a pending request/candidate after acknowledgement until canonical selection. Ensure manager/client construction tests cover create, join/import, and restart load.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/client/group/__tests__/marmot-group.test.ts src/client/__tests__/marmot-client.test.ts src/__tests__/groups-manager.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Public API tests prove enable/disband publish, acknowledgement, rollback, and idempotency; construction tests prove one durable request-store instance reaches every group path and Pending state hydrates before use.</acceptance_criteria>
  <done>Downstream callers can enable legacy groups and request disband through a durable, publish-before-apply public API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated MLS candidate → terminal classification | A forged or mis-parented Commit could kill the group. |
| local caller → durable irreversible request | Repeated or interrupted requests must not create divergent intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-04 | Elevation of privilege | disband validator | critical | mitigate | Parent-relative active-admin check and exact committer leaf/account tests. |
| T-04.1-05 | Tampering | proposal shape | critical | mitigate | Inline-only exact proposal whitelist and full resulting-state invariants. |
| T-04.1-06 | Repudiation | local disband request | high | mitigate | Versioned durable request identity/status before preparation. |
| T-04.1-07 | Denial of service | request regeneration | medium | mitigate | One pending record, one prepared epoch, scheduler-driven retries, typed terminal failures. |
</threat_model>

<verification>Run focused legality/request suites and strict compile; Phase 5 owns the runtime matrix.</verification>
<success_criteria>Exact legality and durable local intent are shared, typed, restart-safe, and publish-before-apply.</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Validate and originate disband candidates | 04.1-02 | COVERED | Candidate-first workflow. |
| REQ | LIFE-01 | Full disband Commit validation | 04.1-02 | COVERED | Shared predicate across seams. |
| REQ | LIFE-02 | Durable local request/regeneration | 04.1-02 | COVERED | Required by canonical spec and MDK. |
| RESEARCH | — | Request survives losing branch and publish failure | 04.1-02 | COVERED | No silent omission. |
| CONTEXT | D-01/D-03 | Durable intent and typed outbound gate | 04.1-02 | COVERED | Public terminal rejection is completed in 04.1-04. |

<output>Create `.planning/phases/04.1-terminal-group-disbanding/04.1-02-SUMMARY.md` when done.</output>
