---
phase: 04.1-terminal-group-disbanding
plan: 05
type: execute
wave: 5
depends_on: [04.1-04]
files_modified:
  - src/engine/disband-tombstone.ts
  - src/engine/types.ts
  - src/client/session/group-session.ts
  - src/client/group/marmot-group.ts
  - src/client/group-registry.ts
  - src/client/groups-manager.ts
  - src/client/marmot-client.ts
  - src/client/group/__tests__/disbanded.test.ts
  - src/client/session/__tests__/group-session.test.ts
  - src/__tests__/groups-manager.test.ts
requirements: [LIFE-02, CONV-05]
autonomous: true
must_haves:
  truths:
    - "LIFE-02/D-01: request and terminal records share one explicitly threaded durable lifecycle store but use separate group-scoped namespaces"
    - "LIFE-02/D-01: selected disband writes the authoritative tombstone before cleanup, and restart cannot resurrect stale live MLS state"
    - "CONV-05/D-07: create, join, import, and load hydrate request/tombstone evidence before activation, and only selected disband becomes terminal"
  artifacts:
    - path: src/engine/disband-tombstone.ts
      provides: "Versioned terminal evidence codec and lifecycle-store tombstone namespace"
    - path: src/client/session/group-session.ts
      provides: "Tombstone-first cleanup and request/tombstone hydration"
    - path: src/client/group-registry.ts
      provides: "Terminal-only hydration without requiring live ClientState"
  key_links:
    - from: src/client/marmot-client.ts
      to: src/client/session/group-session.ts
      via: "same lifecycleStore instance threaded through manager, factory/registry, and group"
      pattern: "lifecycleStore"
    - from: src/client/session/group-session.ts
      to: src/engine/disband-tombstone.ts
      via: "write `${groupId}/disband/terminal` before idempotent cleanup"
      pattern: "disband/terminal"
---

<objective>
Persist and hydrate terminal disband evidence through the same durable lifecycle store as pending requests, while keeping the two record types independently versioned and namespaced.

Purpose: Make D-01's selected terminal state authoritative across partial cleanup and every group construction path.
Output: Tombstone codec, shared-store namespace contract, tombstone-first cleanup, terminal-only hydration, and fault-injection tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-terminal-group-disbanding/04.1-CONTEXT.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-RESEARCH.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-PATTERNS.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-04-SUMMARY.md
@refs/marmot/app-components/group-lifecycle-v1.md
@refs/mdk/crates/cgka-engine/src/disband.rs

<interfaces>
- Plans 02-03 introduce and thread one durable `lifecycleStore: GenericKeyValueStore&lt;Uint8Array&gt;` from MarmotClient through GroupsManager, GroupFactory/GroupRegistry, MarmotGroup, and GroupSession.
- Pending requests use `${groupId}/disband/request`; this plan adds `${groupId}/disband/terminal`. They share the backend instance but have separate versioned codecs and lifecycle rules.
- A terminal tombstone is load authority even when `${groupId}` still contains stale serialized ClientState after interrupted cleanup.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the terminal namespace to the shared durable lifecycle store</name>
  <read_first>refs/marmot/app-components/group-lifecycle-v1.md, refs/mdk/crates/cgka-engine/src/disband.rs, src/client/session/group-session.ts, src/client/marmot-client.ts, src/utils/key-value.ts, src/client/session/__tests__/group-session.test.ts</read_first>
  <decision_record>D-01 is an already user-confirmed one-way tombstone choice; changing the durable terminal contract after release requires migration, so no redundant checkpoint is added.</decision_record>
  <files>src/engine/disband-tombstone.ts, src/engine/types.ts, src/client/session/group-session.ts, src/client/group/__tests__/disbanded.test.ts, src/client/session/__tests__/group-session.test.ts</files>
  <behavior>
    - Pending request is stored only at `${groupId}/disband/request`; terminal evidence is stored only at `${groupId}/disband/terminal` in the identical lifecycleStore instance.
    - Terminal codec retains version, group id, epoch, exact Commit digest, actor, and notification state; unknown/corrupt versions fail closed.
    - Selected disband writes terminal evidence before clearing request/live/history/work records; interrupted cleanup resumes idempotently on hydration.
    - Legacy live state with neither namespace remains active and readable.
  </behavior>
  <action>Reuse the exact durable lifecycle store already threaded in Plans 02-03; do not add another store option or backend. Define separate group-scoped keys `${groupId}/disband/request` and `${groupId}/disband/terminal`, with distinct versioned request/tombstone codecs. On selected disband, write terminal evidence first, then idempotently clear the pending request, live ClientState, rewind/candidate/pending-ingress/effect state, queued work, and timers. Treat terminal evidence as authoritative over stale live bytes and fail closed on corrupt/unknown terminal versions. Preserve only authenticated read-only evidence needed for status/event identity.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/client/session/__tests__/group-session.test.ts src/client/group/__tests__/disbanded.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Namespace isolation, exact shared-store identity, fail-closed codec, tombstone-first ordering, interruption recovery, and legacy live-state behavior pass focused tests.</acceptance_criteria>
  <done>One durable lifecycle store safely retains both unresolved intent and selected terminal evidence without conflating them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hydrate tombstones before activation on every construction path</name>
  <read_first>src/client/marmot-client.ts, src/client/groups-manager.ts, src/client/group-registry.ts, src/client/group/marmot-group.ts, src/__tests__/groups-manager.test.ts, src/client/group/__tests__/disbanded.test.ts</read_first>
  <files>src/client/marmot-client.ts, src/client/groups-manager.ts, src/client/group-registry.ts, src/client/group/marmot-group.ts, src/__tests__/groups-manager.test.ts</files>
  <behavior>
    - Create, join/import, and restart load consult both lifecycle namespaces before activation.
    - Terminal evidence builds a read-only terminal group even when live ClientState is absent; stale live state cannot win.
    - Pending request without terminal evidence hydrates Disbanding and remains eligible for regeneration.
    - Listeners are attached before terminal realization and duplicate concurrent loads share one instance/event identity.
  </behavior>
  <action>At every group construction path, pass the same lifecycleStore instance established in Plan 03 and hydrate `${groupId}/disband/terminal` plus `${groupId}/disband/request` before routing, outbound admission, or convergence scheduling. Extend registry load so terminal evidence can construct a read-only group without deserializing live MLS state, while a pending-only record restores Disbanding. Keep listener-first activation and concurrent-load dedup. Add construction tests to `src/__tests__/groups-manager.test.ts` for create, join/import, terminal-only restart, stale-live-plus-terminal restart, and pending-only restart.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/__tests__/groups-manager.test.ts src/client/group/__tests__/disbanded.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Every construction path hydrates both namespaces before activation, terminal-only load succeeds, stale live state never resurrects, pending-only state regenerates, and listeners/dedup remain correct.</acceptance_criteria>
  <done>Durable lifecycle evidence is authoritative before any public group becomes active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shared lifecycle store → typed request/tombstone | Namespace or codec confusion could erase intent or resurrect a group. |
| tombstone → registry activation | Stale live MLS bytes must never override terminal authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-20 | Tampering | lifecycle-store namespaces | critical | mitigate | Separate fixed keys and codecs with cross-record isolation tests. |
| T-04.1-21 | Information disclosure | terminal cleanup | high | mitigate | Tombstone-first idempotent deletion of live MLS/history/work. |
| T-04.1-22 | Denial of service | terminal-only hydration | high | mitigate | Read-only construction from tombstone without requiring live ClientState. |
</threat_model>

<verification>Run pinned session, terminal, and manager hydration suites plus compile; Phase 5 owns the runtime matrix.</verification>
<success_criteria>One durable lifecycle store, two isolated namespaces, tombstone-first cleanup, and pre-activation hydration prevent restart resurrection.</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable selected terminal state | 04.1-05 | COVERED | Shared-store tombstone authority. |
| REQ | LIFE-02 | Restart-safe tombstone and cleanup | 04.1-05 | COVERED | Separate from pending request record. |
| REQ | CONV-05 | Terminalize selected branch only | 04.1-05 | COVERED | Consumes Plan 04 selected evidence. |
| RESEARCH | — | Tombstone-first cleanup and hydration | 04.1-05 | COVERED | Includes all construction paths. |
| CONTEXT | D-01/D-07 | Durable absorbing selection | 04.1-05 | COVERED | User-confirmed persisted contract. |

<output>Create `.planning/phases/04.1-terminal-group-disbanding/04.1-05-SUMMARY.md` when done.</output>
