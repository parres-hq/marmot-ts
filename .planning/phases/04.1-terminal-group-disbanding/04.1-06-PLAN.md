---
phase: 04.1-terminal-group-disbanding
plan: 06
type: execute
wave: 6
depends_on: [04.1-05]
files_modified:
  - src/engine/types.ts
  - src/engine/ingest.ts
  - src/client/session/group-session.ts
  - src/client/group/marmot-group.ts
  - src/client/group-registry.ts
  - src/client/groups-manager.ts
  - src/client/marmot-client.ts
  - src/client/group/__tests__/disbanded.test.ts
  - src/client/__tests__/disband-routing.test.ts
requirements: [LIFE-02, CONV-05]
autonomous: true
must_haves:
  truths:
    - "LIFE-02/D-02: public group status is disbanded and exactly one actor/digest lifecycle event is emitted after durability"
    - "LIFE-02/D-03/D-04: all outbound and queued work rejects with group_disbanded, and every later inbound envelope is classified inactive before peel without reopening convergence or emitting application output"
    - "CONV-05/D-07: stale live state, history, request/candidate work, timers, and routes cannot override a terminal tombstone"
  artifacts:
    - path: src/client/group/marmot-group.ts
      provides: "Public status, typed terminal error, one-shot event, and queue teardown"
    - path: src/client/group-registry.ts
      provides: "Listener-first tombstone hydration without live MLS reconstruction"
  key_links:
    - from: src/client/group-registry.ts
      to: src/client/group/marmot-group.ts
      via: "terminal-only load path with listeners installed before realization"
      pattern: "disbanded"
    - from: src/client/groups-manager.ts
      to: src/engine/ingest.ts
      via: "early routing gate plus authoritative engine repeat gate"
      pattern: "group_disbanded"
---

<objective>
Expose canonical disband as exactly-once observable, publicly typed, and terminal across routing, inbound, and outbound seams.

Purpose: Complete D-02 through D-04 over Plan 05's authoritative durable tombstone so later transport replay cannot revive a dead group.
Output: Public terminal status/events/errors, queue rejection, inactive routing, and integration tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-terminal-group-disbanding/04.1-CONTEXT.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-RESEARCH.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-PATTERNS.md
@.planning/phases/04.1-terminal-group-disbanding/04.1-05-SUMMARY.md
@refs/marmot/app-components/group-lifecycle-v1.md
@refs/marmot/protocol-core/convergence.md
@refs/mdk/crates/cgka-engine/src/disband.rs
@refs/mdk/crates/traits/src/group.rs
@src/client/session/group-session.ts
@src/client/group/marmot-group.ts
@src/client/group-registry.ts

<interfaces>
- `GenericKeyValueStore&lt;T&gt;` supplies async get/set/remove/list operations and is the only persistence abstraction permitted across supported runtimes.
- `GroupSessionOptions`, `MarmotGroupOptions`, `GroupRegistryOptions`, and `MarmotClientOptions` already thread group state, rewind, ingest evidence, and removed-marker stores.
- `MarmotGroupEvents` and `GroupRegistryEvents` are EventEmitter3 typed maps; registry activation already follows hydration then listener attachment then durable removal realization.
- `RemovedIngestResult` plus `ingestResultDisposition` provide the inactive input analog, but disband must retain a distinct typed terminal reason.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose one public disband status/event and typed terminal outbound behavior</name>
  <read_first>src/client/group/marmot-group.ts, src/client/group-registry.ts, src/client/groups-manager.ts, src/client/group/__tests__/marmot-group.test.ts, src/__tests__/integration/removed.test.ts, src/engine/state-notifications.ts</read_first>
  <files>src/engine/types.ts, src/client/session/group-session.ts, src/client/group/marmot-group.ts, src/client/group-registry.ts, src/client/group/__tests__/disbanded.test.ts</files>
  <behavior>
    - `status` distinguishes active, removed, and disbanded from tombstone authority.
    - Selected disband emits one group-level and manager-level actor/digest event only after persistence; restart emits no duplicate and listener exceptions cannot roll back completion.
    - All app-message, Commit, maintenance, request, and queued-drain calls reject with exported `group_disbanded`; pending queues are drained once.
    - Registry hydration attaches listeners before terminal realization and never reconstructs a live group merely because ClientState was deleted.
  </behavior>
  <action>Implement D-02/D-03 with named exported `MarmotGroupStatus` and a typed `GroupTerminalError` or equivalent discriminated result carrying `reason: "group_disbanded"`. Emit one actor/digest `disbanded` event from `MarmotGroup` and forward it through registry only after tombstone persistence and notification-delivery recording; isolate application callback failures. Load a read-only terminal group directly from tombstone evidence with listeners attached first. Reject all app-message, Commit, maintenance, request, and queued-drain work and drain pending callers exactly once. Suppress member/admin/component presentation rows from the terminal Commit. Keep removed and disbanded distinct public reasons, and do not treat Unrecoverable as terminal.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/client/group/__tests__/disbanded.test.ts src/engine/__tests__/disband-convergence.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Public status/event, exactly-once restart behavior, callback isolation, typed refusal across every operation, and presentation suppression pass focused tests.</acceptance_criteria>
  <done>Downstream callers can reliably observe group death once and every outbound path refuses it with one typed reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop terminal routing and classify all later inbound before peel</name>
  <read_first>refs/mdk/crates/traits/src/group.rs, refs/mdk/crates/marmot-app/src/client/mod.rs, src/client/groups-manager.ts, src/client/marmot-client.ts, src/engine/ingest.ts, src/__tests__/groups-manager.test.ts</read_first>
  <files>src/engine/ingest.ts, src/engine/types.ts, src/client/groups-manager.ts, src/client/marmot-client.ts, src/client/__tests__/disband-routing.test.ts</files>
  <behavior>
    - Fresh, duplicate, and malformed late inbound are classified terminal/inactive before peeler invocation, create no output/pass/timer, and cannot update public state.
    - Terminal group subscriptions are stopped immediately and are not re-seeded by manager/client reconstruction.
    - Manager-level disbanded notification forwards the same actor and Commit digest once.
    - Removed and disbanded both exclude routing and sending but retain distinct reasons; Unrecoverable remains repairable and routed according to existing policy.
  </behavior>
  <action>Implement D-04 at both trust boundaries: gate group routing before subscription/peel and repeat the terminal check in engine ingress for direct callers, mapping late input to a terminal-specific member of the stale/inactive disposition family. Stop/unsubscribe group transport ownership and ensure client/manager rebuilding cannot seed a tombstoned route. Forward the durable group event through `GroupsManager`. Apply the updated MDK `Group::is_terminal` lesson without importing app-layer code: removed and disbanded share route/send exclusion, remain distinguishable, and Unrecoverable is not terminal.</action>
  <verify><automated>CI=true npx --yes pnpm@10.18.3 vitest run src/client/__tests__/disband-routing.test.ts src/client/group/__tests__/disbanded.test.ts src/__tests__/groups-manager.test.ts &amp;&amp; CI=true npx --yes pnpm@10.18.3 compile</automated></verify>
  <acceptance_criteria>Pre-peel inactive classification, zero application/convergence side effects, immediate route teardown, restart route exclusion, and one manager event all pass focused tests.</acceptance_criteria>
  <done>A terminal group cannot consume transport input or regain a route after selection or restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical engine outcome → durable stores | Partial multi-store cleanup must never resurrect terminal state. |
| persisted tombstone → public API | Corrupt evidence must fail closed and valid evidence must retain actor identity. |
| Nostr routing → terminal group | Untrusted late traffic must stop before crypto/history/scheduler work. |
| library event → application callback | Throwing listeners must not undo durable terminalization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-12 | Tampering | tombstone/load ordering | critical | mitigate | Tombstone-first authority plus interruption matrix and idempotent cleanup. |
| T-04.1-13 | Spoofing | public actor attribution | high | mitigate | Persist authenticated candidate actor/digest only after canonical selection. |
| T-04.1-14 | Denial of service | late inbound routing | high | mitigate | Client and engine terminal gates before peel/pass/timer allocation. |
| T-04.1-15 | Repudiation | exactly-once lifecycle event | high | mitigate | Durable delivery identity keyed by selected Commit digest before callback. |
| T-04.1-16 | Information disclosure | retained live MLS state | high | mitigate | Idempotently delete live state/history/work after tombstone authority is durable. |
</threat_model>

<verification>Run public terminal I/O, routing, and convergence integration suites plus strict compile; Phase 5 remains responsible for all-runtime execution.</verification>
<success_criteria>Selected disband is durable, absorbing, exactly-once observable, typed, and inert across all later I/O.</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Observable terminal I/O state | 04.1-06 | COVERED | Public API, queue rejection, routing, and inbound gates. |
| REQ | LIFE-02 | Public absorbing terminal contract | 04.1-06 | COVERED | Consumes Plan 05 tombstone authority. |
| REQ | CONV-05 | Expose only selected terminal branch | 04.1-06 | COVERED | Consumes Plans 04-05 selected evidence. |
| RESEARCH | — | Terminal route/send gate | 04.1-06 | COVERED | Includes latest MDK route-teardown finding. |
| CONTEXT | D-02/D-03/D-04/D-07 | Observation, I/O, selection | 04.1-06 | COVERED | Each maps to automated behavior. |

<output>Create `.planning/phases/04.1-terminal-group-disbanding/04.1-06-SUMMARY.md` when done.</output>
