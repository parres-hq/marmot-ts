---
phase: 04.1-terminal-group-disbanding
verified: 2026-09-06T17:39:00Z
status: passed
score: 18/18 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 17/18
  gaps_closed:
    - "Forced disband admission now preserves an existing pass's quiescence anchor and complete pass identity."
    - "Public legacy enablement now has a real unsupported-leaf/no-publication regression."
    - "Durable request membership/admin authority-loss transitions now have behavioral coverage."
    - "Real public active-winner selection automatically regenerates pending disband; terminal selection suppresses further regeneration."
  gaps_remaining: []
  regressions: []
---

# Phase 04.1: Terminal Group Disbanding Verification Report

**Phase Goal:** Implement `marmot.group.lifecycle.v1` and terminal `disbanded`, with every disband candidate admitted through bounded convergence.
**Verified:** 2026-09-06T17:39:00Z
**Status:** passed
**Re-verification:** Yes — final closure after `02a00c1` and `26e0fa0`

## Goal Achievement

### Roadmap Success Criteria

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Lifecycle-v1 is byte-exact and participates in required-component integrity | VERIFIED | `group-lifecycle.ts:14-40` encodes `00`/`01` and rejects trailing/unknown input; IDs/default/capability lists are in `ids.ts:40,61-86`; dictionary, malformed-view, integrity, and MDK KeyPackage byte tests pass. |
| 2 | A valid linear disband forces `Stable -> Recovering` and terminalizes only after selection | VERIFIED | Inbound legality precedes admission (`ingest.ts:724-769`); `#admitDisbandCandidate` retains rather than applies the edge (`group-engine.ts:2348-2391`); named local and inbound linear tests hold the parent epoch until cutoff and then enter `Disbanded`. |
| 3 | Forced admission changes no timer/base/pass identity/scoring priority | VERIFIED | `c0006a7` updates `#admitDisbandCandidate` only to initialize timing when no pass exists. The regression opens an ordinary pass at t=100, admits disband at t=500, and asserts complete pass equality; named test passes. Standard scoring remains unchanged. |
| 4 | Canonical disband is terminal for outbound and later inbound | VERIFIED | Tombstone authority gates every public outbound operation and rejects queued work (`marmot-group.ts:776-802`); engine ingest short-circuits before peel (`ingest.ts:347-353`); focused terminal and routing tests pass. |

### Plan Must-Haves

| Area | Status | Evidence |
|---|---|---|
| Exact codec, defaults, legacy read, public projection | VERIFIED | Exact vectors, new group dictionary/view, legacy absence, and malformed-present fail-closed tests passed. |
| Parent-relative exact Commit legality shared by send/inbound/replay | VERIFIED | `classifyDisbandCommit` validates parent admin, required lifecycle, inline updates, exact remaining leaf/admin, and forbidden proposals; `validateCommitLegality` is consumed at all three seams. |
| Required-component immutability/integrity | VERIFIED | Shared integrity suite (23 tests) passed; disband validation rejects removal/invalid transition shapes through the common adapter. |
| All-resulting-leaf capability requirement | VERIFIED | `4db6b46` builds a real two-leaf legacy state, removes lifecycle support from one leaf, asserts typed `unsupportedMembers`, and asserts the network published zero events. |
| Durable request, publish-failure/restart retry, outbound gate | VERIFIED | Request record is persisted before preparation; `disband-request.test.ts` proves failure retry and hydration gate. |
| Active-winner regeneration and typed authority loss | VERIFIED | `c9374ba` proves durable `NoLongerMember`/`NoLongerAdmin`; `02a00c1` drives a real deeper active competitor through public convergence and observes a second publication with `lastPreparedEpoch: 2` without repeating the API call. |
| One lifecycle store across create/join/import/load, distinct namespaces | VERIFIED | Composition-root wiring and namespace codecs verified; construction/hydration tests pass. |
| Fresh and local-confirmed linear disband convergence | VERIFIED | Named local and inbound tests assert `Recovering`, unchanged canonical epoch, cutoff selection, and terminalization. |
| Existing-pass immutable timers/base/pass identity | VERIFIED | Fix preserves an existing pass untouched; regression asserts object equality after later disband admission. |
| No terminal scoring priority | VERIFIED | `fork-recovery.ts:552-585` selects/scores all branches with the ordinary comparator before looking up terminal metadata. |
| Canonical-only terminalization / active selection remains non-terminal | VERIFIED | `selectedTerminal` is derived only after ordinary winner selection; engine transitions to `Disbanded` only when that selected evidence exists. |
| Tombstone-first durable terminalization and restart-safe cleanup | VERIFIED | Terminal write precedes scrubbed shell and cleanup (`group-session.ts:460-519`); fault tests cover shell-write interruption and two subsequent restarts. |
| Convergence evidence write-ahead and torn history recovery | VERIFIED | `save()` writes self-contained convergence evidence before history (`group-session.ts:360-376`); injected history failure restores `Recovering` and repairs the edge. |
| Public status, actor/digest event, exactly-once delivery | VERIFIED | Delivery is persisted before callbacks (`group-session.ts:429-447`); public test proves one actor/digest event despite a throwing listener and repeated realization. |
| Terminal outbound/queue/inbound/routing behavior | VERIFIED | Typed `group_disbanded` rejection, queue teardown, pre-peel inactive classification, terminal registry discovery, and route exclusion tests pass. |

**Score:** 18/18 truths verified (0 present but behavior-unverified)

### Required Artifacts and Key Links

All 17 declared production artifacts exist and are substantive. Manual inspection confirms the declared codec/dictionary/view, legality/integrity, lifecycle-store, convergence, tombstone, registry, and public facade links are wired. The metadata query's two negative results are parser/pattern false negatives: the four lifecycle exports are individually present, and terminal inbound routing uses `group-disbanded` at the engine boundary rather than the public exception token `group_disbanded` in `GroupsManager`.

### Data-Flow Trace

| Data | Source | Sink | Status |
|---|---|---|---|
| Lifecycle bytes | authenticated GroupContext dictionary | `MarmotGroupView.protocolLifecycle` | FLOWING |
| Pending request | `${groupId}/disband/request` | engine gate and regeneration | FLOWING |
| Candidate/pass evidence | lifecycle write-ahead record | restart hydration/history repair | FLOWING |
| Selected terminal evidence | `${groupId}/disband/terminal` | registry facade, status, event, I/O gates | FLOWING |

### Behavioral Spot-Checks

| Check | Result | Status |
|---|---|---|
| Initial 13 focused codec/integrity/request/convergence/session/client/registry test files | 118 tests passed | PASS |
| Final re-verification: 5 focused convergence/request/public/terminal/routing files | 27 tests passed in 2.61s | PASS |
| `pnpm compile` | exit 0 | PASS |
| Existing-pass disband admission keeps complete pass identity | Ordinary pass created before later candidate; `convergencePass` equality assertion passes | PASS |
| Unsupported legacy leaf causes no publish | Typed rejection and empty mock-network event list pass | PASS |
| Authority loss after pending request | `NoLongerMember` and `NoLongerAdmin` durable failure assertions pass | PASS |
| Automatic regeneration after a selected active branch | Real two-commit competing branch wins; public scheduler publishes regenerated disband without another API call | PASS |
| Terminal winner suppresses regeneration | Regenerated cutoff yields `status: disbanded`; network remains at exactly two publications | PASS |

### Probe Execution

No Phase 04.1 probe script is declared or present. Probe execution was not applicable.

### Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| LIFE-01 | SATISFIED | Codec/default/legacy/integrity/exact Commit validation and real unsupported-leaf/no-publish behavior are implemented and tested. |
| LIFE-02 | SATISFIED | Durable/public/terminal/crash, typed authority loss, automatic active-winner regeneration, and terminal-winner suppression are behaviorally tested. |
| CONV-05 | SATISFIED | Linear/local admission, immutable existing-pass timing/base/identity, ordinary scoring, and canonical-only terminal selection are implemented and tested. |

No Phase 04.1 requirement mapped in `REQUIREMENTS.md` is orphaned from the plans.

### Anti-Patterns Found

No unreferenced `TBD`, `FIXME`, or `XXX` markers, placeholders, or empty implementations were found in the phase production files. `git diff --check` and strict compilation passed.

### Human Verification Required

None. Every behavior-dependent lifecycle transition has passing automated evidence.

### Gaps Summary

All prior gaps and behavior-evidence warnings are closed. The public path now persists scheduler-selected terminal evidence before realization, regenerates only after an active winner, and does not regenerate after a terminal winner. Previously verified codec, integrity, terminal I/O, tombstone, event, routing, and torn-write behavior showed no regression.

Disconfirmation pass: the end-to-end test proves both sides of the outcome boundary—an active winner produces exactly one replacement publication, while the terminal winner persists and realizes `disbanded` with no third publication. No partial must-have, misleading phase test, or uncovered terminal error path remains.

---

_Verified: 2026-09-06T17:39:00Z_
_Verifier: the agent (gsd-verifier)_
